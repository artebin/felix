#!/usr/bin/env sh

# The shell used to execute the .xsessionrc is the one used by the graphical login session manager.
# It is safe to assume that it will be sh.

if [ -d ${HOME}/.xsessionrc.d ]; then
	for FILE in ${HOME}/.xsessionrc.d/*; do
		bash "${FILE}"
	done
fi

