tint2 &

# xfce4-volumed could be started via /etc/xdg/autostart/xfce4-volumed.desktop but there is a parameter "OnlyShowIn=XFCE;"
xfce4-volumed &

pasystray &

# Register Thunar in D-BUS
#thunar --daemon &

# Keyboard should be set properly in /etc/default/keyboard
#setxkbmap -rules evdev -model evdev -layout us -variant altgr-intl &

/usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1 &

# Do not call `xmodmap` from here, it will not work. Use `~/.xinitrc` instead.
#xmodmap ~/.Xmodmap &

xsetroot -solid "#303030" &

# If distribution is Xubuntu then xfce4-power-manager is started via /etc/xdg/autostart/xfce4-power-manager
# Could be the file should be renamed in order to prevent xfce4-power-manager to be started

mate-power-manager &

clipmenud &
